# Deps added per stage, not all up front.
anthropic>=0.106
langgraph>=1.2
python-dotenv>=1.2

# Stage 1 — testing
pytest>=9.0

# Stage 1.6 — Module 02 retrieval (pinned to match the venv that built the index;
# the query embedder MUST match the index's embedder or recall silently degrades).
# Module 02 itself is imported from ../module-02-rag-app (MODULE_02_PATH), not pip.
numpy==2.4.6
torch==2.12.0
chromadb==1.5.9
sentence-transformers==5.5.1

# Stage 3 — MCP (Model Context Protocol): our client + a local market-data server.
# (Live prices come from Yahoo via stdlib urllib in prices.py — no extra dep.)
mcp>=1.0
